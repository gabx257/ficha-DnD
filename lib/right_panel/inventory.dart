import 'package:ficha/core/dropdownselection.dart';

// Inventory is the base widget for the inventory tab
class Inventory extends ListingBox {
  const Inventory(
      {super.key, required super.list, super.title, super.controller});
}
